<h2 class="m-t-1">大会一覧</h2>

<table class="table table-hover">
  <thead>
    <tr>
      <th>大会名</th>
    </tr>
  </thead>
  <tbody>
  <% @events.each do |event| %>
    <tr>
      <td><%= link_to event.name, event_path(event) %></td>
    </tr>
    <% end %>
  </tbody>
</table>
